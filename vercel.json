{
  "buildCommand": "npm install --prefix book && npm run build --prefix book",
  "outputDirectory": "book/build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://rag-backend-production-50e0.up.railway.app/api/$1" }
  ]
}
