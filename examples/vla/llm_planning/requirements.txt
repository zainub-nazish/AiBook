# LLM Planning Requirements
# Supports both local (Ollama) and cloud (OpenAI/Anthropic) APIs

# Local LLM via Ollama
ollama>=0.1.0

# Cloud APIs (optional - choose one)
openai>=1.0.0
anthropic>=0.18.0

# JSON schema validation
pydantic>=2.0.0
jsonschema>=4.20.0

# HTTP client for API calls
httpx>=0.25.0
requests>=2.31.0

# Environment variable management
python-dotenv>=1.0.0

# For ROS 2 integration
# rclpy  # Install via apt: ros-humble-rclpy

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
